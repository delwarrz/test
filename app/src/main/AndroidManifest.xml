<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="info.delwarhossain.bjitacademyhometask">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:extractNativeLibs="true"
        android:theme="@style/Theme.BJITAcademyHomeTask">
        <activity
            android:name=".SplashScreen"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/app_name"
            android:theme="@style/Theme.BJITAcademyHomeTask.NoActionBar.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BaseActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".NoteAddActivity" />
        <activity android:name=".EmiCalculator.EmiCalculatorInputActivity" />
        <activity
            android:name=".EmiCalculator.EmiCalculationResultActivity"
            android:label="EMI Calculation Result" />
        <activity
            android:name=".EmiCalculator.EmiInstallmentChartActivity"
            android:label="Installment Chart" />
        <activity android:name=".FinancialTermActivity"></activity>
    </application>

</manifest>